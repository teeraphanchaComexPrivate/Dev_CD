name: This is first CD
on:
  push:
    branches:
      - main

jobs:
  created_file_on_hosts:
    runs-on: self-hosted
    steps:
      - name: Step 1 Checkout code 
        uses: actions/checkout@v4
        name: Check Cursor
        run: pwd
      - name: Step 2 Copy To Deploy
        run: cp index.html /var/www/html3
      #- name: Generated_files
       # run: echo "peter" >> /home/peter/testgit.txt
